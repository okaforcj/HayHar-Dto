type LandedUser = {
   firstName: string;
   lastName: string;
   email: string;
   uid: string;
   orderedProducts?: String[];
   wishlist?: string[]
   photoUrl?: string;
}

export default LandedUser;