name: "🚀 release"

on:
  push:
    branches:
      - master

jobs:
  release:
    name: 🚀 release
    runs-on: ubuntu-latest
    steps:
      - name: 📚 checkout
        uses: actions/checkout@v2.1.1
      - name: 🟢 node
        uses: actions/setup-node@v2
        with:
          node-version: 12
          registry-url: https://registry.npmjs.org
      - name: 🚀 publish
        run: npm version minor && npm run build && npm publish --access public
        with:
          git_email: okaforcj@icloud.com
        env:
          NODE_AUTH_TOKEN: ${{secrets.NPM_AUTH_TOKEN}}

